{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/library/wrappers/dotnet/QuickLogger/QuickLogger.DefaultSettings.Generator/QuickLogger.DefaultSettings.Generator.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}